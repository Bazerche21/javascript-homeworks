function tellStory([name, mood, activity]) {
    return alert(`This is ${name}. ${name} is a nice person 
    Today they are ${mood}. They are ${activity} all day. The end.`)
}

tellStory(["Matej", "happy", "working"])